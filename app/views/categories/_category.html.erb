<div id="<%= dom_id category %>">

<p>
    <strong>Author:</strong>
    <%= category.user.name %>
  </p>
  <p>
    <strong>Category Name:</strong>
    <%= category.name %>
  </p>

  <p>
    <strong>Icon:</strong>
    <%= category.icon %>
  </p>

  <p>
    <strong>Total transactions:</strong>
    $<%= category.expenses.inject(0) { |total, e| total + (e.amount) }%>
  </p>

</div>
